class LeaderBoard {
    constructor() {
        this.members = []; //an empty array of members we will add them in the future
    }
    addMember(Race)

    sortByDistance();
    sortByBestPace();
    sortByVo2Mac();
}